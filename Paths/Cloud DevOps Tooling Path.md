# Cloud DevOps Tooling Path

## Mục tiêu

Path này giúp người đọc đi qua Cloud DevOps Tooling theo thứ tự học hoặc làm project.

## Khi nào dùng path này

- Khi cần hiểu hoặc mở rộng vùng Cloud / DevOps Tooling.
- Khi thiết kế, debug, deploy hoặc audit hệ thống liên quan.
- Khi muốn đi theo route thay vì mở node ngẫu nhiên.

## 1. Infrastructure as code

- [[GitOps]] — node nền để đi qua stage infrastructure as code.
- [[Terraform State]] — node nền để đi qua stage infrastructure as code.
- [[Terraform Plan]] — node nền để đi qua stage infrastructure as code.
- [[Terraform Apply]] — node nền để đi qua stage infrastructure as code.
- [[Terraform Module]] — node nền để đi qua stage infrastructure as code.

## 2. Delivery pipeline

- [[GitHub Actions]] — node nền để đi qua stage delivery pipeline.
- [[Pipeline Runner]] — node nền để đi qua stage delivery pipeline.
- [[Build Cache]] — node nền để đi qua stage delivery pipeline.
- [[Artifact Repository]] — node nền để đi qua stage delivery pipeline.
- [[Artifact Signing]] — node nền để đi qua stage delivery pipeline.

## 3. Runtime platform

- [[Container Runtime Interface]] — node nền để đi qua stage runtime platform.
- [[Ingress Controller]] — node nền để đi qua stage runtime platform.
- [[Service Mesh]] — node nền để đi qua stage runtime platform.
- [[Sidecar Proxy]] — node nền để đi qua stage runtime platform.
- [[Helm Chart]] — node nền để đi qua stage runtime platform.

## 4. Observability

- [[OpenTelemetry]] — node nền để đi qua stage observability.
- [[Metric Backend]] — node nền để đi qua stage observability.
- [[Log Aggregation]] — node nền để đi qua stage observability.
- [[Trace Sampling]] — node nền để đi qua stage observability.
- [[Dashboard]] — node nền để đi qua stage observability.

## 5. Operations

- [[Runbook Automation]] — node nền để đi qua stage operations.
- [[Deployment Approval]] — node nền để đi qua stage operations.
- [[Progressive Delivery]] — node nền để đi qua stage operations.
- [[Blue Green Switch]] — node nền để đi qua stage operations.
- [[Canary Analysis]] — node nền để đi qua stage operations.

## Missing / Future nodes

Các khái niệm nên bổ sung sau, ghi text thường, không wikilink:

- Case studies
- Vendor-specific services
- Tool-specific commands

## Related paths

- [[Backend Engineering Path]]
- [[Cloud Infrastructure Path]]
- [[Debugging and Failure Patterns Path]]
